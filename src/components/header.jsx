const Hero = ({title, subtitle, subtitle1}) => {
  return (
    <>
    <header>
      <h1>{title}</h1>
      <p>{subtitle}</p>
      <p>{subtitle1}</p>
    <button>Button</button>
    </header>
    </>
  )
}

export default Hero
